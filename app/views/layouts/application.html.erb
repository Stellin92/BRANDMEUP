<!DOCTYPE html>
<html>
  <head>
    <title>Brandmeup</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Belleza&family=Julius+Sans+One&family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4Q6Gf2aSP4eDXB8Miphtr37CMZZQ5oXLH2yaXMJ2w8e2ZtHTl7GptT4jmndRuHDT" crossorigin="anonymous">
    <%= javascript_importmap_tags %>
  </head>

  <body data-controller="inbox">
    <%= render "shared/navbar" %>
    <%= render "shared/flashes" %>
    <main class="page-content">
        <%= yield %>
    </main>
    <%= render "shared/footer" %>
    <div class="inbox-overlay"
        data-inbox-target="overlay"
        data-action="click->inbox#close"></div>

    <div id="inbox-drawer"
        class="inbox-drawer"
        data-inbox-target="drawer">
      <% if user_signed_in? %>
        <div class="inbox-header">
          <div class="inbox-title">Inbox</div>
          <button class="btn btn-sm btn-outline-secondary ms-auto" data-action="inbox#close">Close</button>
        </div>
        <%= turbo_frame_tag "inbox",
              data: {
                inbox_target: "frame",
                src: user_chats_path(current_user)
              } %>
      <% else %>
        <%= turbo_frame_tag "inbox", data: { inbox_target: "frame" } %>
      <% end %>
    </div>
  </body>
</html>
